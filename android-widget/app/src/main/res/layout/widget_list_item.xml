<?xml version="1.0" encoding="utf-8"?>
<!-- One row in the scrollable task list. Two independent tap targets:
     btn_check marks the task complete, tv_title opens the captured link
     (or the app if there isn't one) — tapping to read never marks anything
     done, and marking done never requires opening the link first. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="7dp"
    android:paddingBottom="7dp">

    <TextView
        android:id="@+id/btn_check"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:gravity="center"
        android:text="○"
        android:textSize="15sp"
        android:textColor="#e44332" />

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="6dp"
        android:layout_marginEnd="8dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:textColor="#1a1a1a"
        android:textSize="13sp"
        android:maxLines="2"
        android:ellipsize="end" />

</LinearLayout>
